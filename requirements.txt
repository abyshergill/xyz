Django>=6.0,<6.1
Pillow>=11.0
qrcode[pil]>=8.0
reportlab>=4.0        # generates downloadable PDF order receipts/bills
django-environ>=0.14
argon2-cffi>=25.0
psycopg2-binary>=2.9   # PostgreSQL driver, only needed if you uncomment the Postgres block in config/settings.py
gunicorn>=23.0          # production WSGI server (paired with Caddy as reverse proxy)
whitenoise>=6.7          # optional: simplifies static file serving without a separate nginx/Caddy static route
django-redis>=5.4.0
redis>=5.0.0
argon2-cffi-bindings==26.1.0
asgiref==3.12.1
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
django-allauth==65.19.2
PyJWT==2.13.0
requests==2.34.2
